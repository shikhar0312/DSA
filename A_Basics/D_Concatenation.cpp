#include <iostream>
using namespace std;
int main()
{
    int x = 10, y = 15, z = 20;
    cout << "The value of x is " << endl;
    // Here we cann't concatenate like we did in java using +
    cout << 'a' + 'b';
    // If we try t concatenate 2 characters then it adds there ASCII Value
}
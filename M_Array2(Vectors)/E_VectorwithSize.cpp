#include <iostream>
using namespace std;

int main()
{
    // Allocating the size of vector
    vector<int> v(5);
    // cout << v[3] << endl;

    // Allocating the size of vector and assining 3 in each size
    vector<int> p(5, 3);
    // cout << p[3] << endl;

    vector<int> pp;

    pp.push_back(1);
    pp.push_back(2);
    pp.push_back(3);
    pp.push_back(4);
    pp.push_back(5);

       // If we declare the size of an vector then we can input them just like array
}